<Window x:Class="WPFLibrary.EditUserWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WPFLibrary"
        mc:Ignorable="d"
        Title="Editing User" MinHeight ="260" MinWidth="400" Height="260" Width="400">


    <Border Padding="10">
        <StackPanel>

            <!-- Title-->
            <TextBlock Text="Editing user" FontWeight="Bold" Margin="0 10" />

            <!-- Name -->
            <TextBlock Text="Name" />
            <TextBox x:Name="NameText" Padding="2" />

            <!-- Surname -->
            <TextBlock Text="Surname" Margin="0 10 0 0" />
            <TextBox x:Name="SurnameText" Padding="2" />

            <!-- Buttons -->
            <Button x:Name="UpdateUserButton" Click="UpdateUserButton_Click" Margin="0 20 200 10" Grid.Column="0" Content="Update User"/>
            <Button x:Name="CloseButton" Click="CloseButton_Click" Margin="0 0 200 10" Grid.Column="0" Content="Close"/>
        </StackPanel>
    </Border>
</Window>
